# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# ========================================
# Database Configuration
# ========================================

# Local Development (Docker PostgreSQL)
# Matches docker-compose.yml postgres service configuration
DATABASE_URL="postgresql://postgres:password@localhost:5432/insurflow"

# Optional: POSTGRES_PORT controls the local docker Postgres host port mapping (default: 5432)
# If port 5432 is occupied, set POSTGRES_PORT=5433 in .env and update DATABASE_URL accordingly
# POSTGRES_PORT=5432

# Production Database (Neon Cloud)
# Uncomment and configure for production deployment
# DATABASE_URL="postgresql://username:password@host/dbname?sslmode=require"

# ========================================
# Better Auth Configuration
# ========================================

# Secret used by Better Auth (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET=""

# Base URL for Better Auth (used for OAuth callbacks)
# For local development: http://localhost:3000
# For production: https://your-domain.com
# For Vercel previews: leave unset (auto-detected from VERCEL_URL)
BETTER_AUTH_URL="http://localhost:3000"

# Better Auth GitHub OAuth (optional)
BETTER_AUTH_GITHUB_CLIENT_ID=""
BETTER_AUTH_GITHUB_CLIENT_SECRET=""

# ========================================
# AI/LLM Configuration (for GenAI Co-Pilot)
# ========================================

# OpenAI API Key (for GPT models)
# OPENAI_API_KEY=""

# Google API Key (for Gemini models)
# GOOGLE_API_KEY=""

# ========================================
# Third-Party Services
# ========================================

# OpenTelemetry / Grafana Cloud (Structured Logging & Tracing)
# Get your credentials from https://grafana.com/products/cloud/
# 1. Go to My Account → Grafana Cloud Portal → Your Stack → Details
# 2. Find OpenTelemetry section and click Configure
# 3. Copy the endpoint and generate an API token
OTEL_SERVICE_NAME="insurflow"
# OTEL_EXPORTER_OTLP_ENDPOINT="https://otlp-gateway-prod-us-central-0.grafana.net/otlp"
# GRAFANA_CLOUD_INSTANCE_ID=""
# GRAFANA_CLOUD_API_TOKEN=""

# Optional: Trace sampling rate (0.0-1.0, default: 1.0 in dev, 0.2 in prod)
# OTEL_TRACES_SAMPLER_ARG="0.2"

# Sentry (Error Tracking)
# Get your DSN from https://sentry.io/settings/projects/
# SENTRY_DSN=""
# NEXT_PUBLIC_SENTRY_DSN=""
# For source maps upload (optional)
# SENTRY_AUTH_TOKEN=""
# SENTRY_ORG=""
# SENTRY_PROJECT=""

# PostHog (Product Analytics)
# Get your project key from https://app.posthog.com/project/settings
# NEXT_PUBLIC_POSTHOG_KEY=""
# NEXT_PUBLIC_POSTHOG_HOST="https://app.posthog.com"

# Stripe (Payments & Subscriptions)
# STRIPE_SECRET_KEY=""
# STRIPE_WEBHOOK_SECRET=""
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""

# UploadThing (File Storage)
# UPLOADTHING_SECRET=""
# UPLOADTHING_APP_ID=""
